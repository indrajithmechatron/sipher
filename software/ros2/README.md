# ROS 2 Middleware Layer

**Layer 3** of the Sipher architecture. See `docs/ARCHITECTURE.md` for full spec.

## Status

Scaffolding — ROS 2 Jazzy Jalisco package `sipher_hardware` to be created.

## Planned Topics

- `/imu/data` — `sensor_msgs/Imu`
- `/gps/fix` — `sensor_msgs/NavSatFix`
- `/battery/status` — custom msg
- `/cmd_vel` — `geometry_msgs/Twist` (subscribe)
